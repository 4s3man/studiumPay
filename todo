najświeższe
  zrobić event usuwający do bazy w repozytorium
  zrobić w końcu testy jakoś :(
  ??zrobić tak żeby form zwracał obiekt "order" który implementuje interfejsy 'parseForPrzlewy24TrnRegister', i 'parseForDatabaseSave'?
  

problemy z zapisem do bazy:
  !!
    (wydarzenia: id, nazwa, itp) -> form renderuje nazwy i podaje id ->
    zapis do bazy + rejestracja eventu usuwającego po jakimś czasie wszystkie rzeczy z bazy łączące się z danym_order_id które nie są zwalidowane
    przelewy24 robią z tego $data do przelewów
    rejestrują transakcję
    czytanie z bazy i weryfikacja transakcji,
    validowanie rzeczy w tablicach
  !!


form Handling
  new session id before form
  validate transaction / make and debug -- potrzeba to zrobić nie na lokal hoście albo pozwolić postować przelewom na mój komp
  save validated transaction data



step 2
form
 make inputs from tribe events
 js dynamically set amount based on selected inputs
